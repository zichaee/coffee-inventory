import {
  createTheme,
} from "@mui/material";

export const customTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#508330',
    },
    secondary: {
      main: '#9b620b',
    },
  },
});
